variables initialization
initializeVariablesIfNeeded
	"If this shared pool shared pool should be initialized, then lookup for an autogenerated init method for this platform. If found, then initialize from it. If not found, then do nothing."
	| selector | 
	self shouldInitializeVariables ifTrue: [ 
		selector := self lookupInitMethodForCurrentPlatform.
		selector ifNotNil: [ 
			self initializeVariablesFrom: selector.
			self updatePlatformInfo.
		] 
	]
	